<% if @songs.any? %>
  <% @songs.each do |song| %>
    <h4><a href="/songs/<%=song.id%>"><%=song.title%><a></h4>
    <p><a href="/users/<%= song.user_id%>"><%=song.user.username%></a></p>
  <% end %>
  <% else %>
    <h4>There are no results found for your search</h4>
<% end %>